const gameButton = document.getElementById("btn-pipisnica");
if (gameButton) {
    gameButton.addEventListener('click', () => {
        window.location.href = "projects/pipisnica/choiseTheme.html";
    });
}